# 魔镜 MagicGlass v0.3.5 用户指南

## 它能做什么

魔镜会在你明确开启“记忆”后，周期观察当前视野，把可寻找物品的场景、语义地点和相对位置保存在眼镜本地。之后可输入或说出“我的钥匙在哪里？”，魔镜只返回最近一次可靠记录，不会猜测位置。

## 导入与打包

1. 将本 GitHub 仓库导入 Rokid Craft Web 工作台，工程根目录应直接包含 `AGENTS.md`、`app.js`、`app.json` 和 `pages/`。
2. 先运行 Preview。Web/Craft 环境缺少 Camera、LanguageModel 或 SpeechRecognition 时，首页仍应正常显示；DEBUG 区会将对应能力标为 `N/A`。
3. 在 Craft 中执行 AIX 打包，不需要选择子目录。
4. 上传生成的 AIX 到 MagicGlass 智能体。
5. 在 Rokid AI App 中打开开发者模式，将智能体同步到 Rokid Glasses 后启动“魔镜”。

## 第一次使用

1. 启动后观察 DEBUG 区，真机理想状态为 `Camera Ready · LLM Ready · ASR Ready · TTS Ready`。
2. 点击“立即观察”，看向摆有钥匙、手机或水杯的桌面。
3. 等待提示从“正在观察”变为“记住了 N 个物品”。最近物品区应出现识别结果。
4. 移动到另一个位置，再次观察同一物品，用于验证 Last Seen。
5. 输入“钥匙在哪里”，或点击“语音”后说出该句。
6. 结果卡会展示最后看到时间、场景、具体位置和简短寻找提示。

## 眼镜交互

- 首次按确认键进入页面导航模式。
- 使用眼镜的上/下方向操作在按钮和输入框之间移动。
- 当前选中控件会出现白色高亮轮廓；再次按确认键执行操作。
- “最近看到”中的物品也是可选按钮，选中并确认后会直接查询该物品。
- 返回键交给系统默认行为，用于退出当前导航层级或关闭页面。

## 周期记忆

- 点击“开始记忆”后会立即观察一次；页面保持前台时，随后约每 30 秒继续观察。
- 点击“停止记忆”会立即停止周期观察。
- 页面隐藏、退出或卸载时会停止 Timer 和语音能力。当前版本不承诺后台持续拍摄。
- 如果上一轮仍在拍摄或分析，下一次周期任务会跳过，绝不并发采集。

## 隐私与数据

- 拍摄图片仅用于当次视觉分析，不长期保存。
- 本地仅保存结构化 Observation，最多 200 条，超出后自动移除最旧记录。
- DEBUG 区“清空真实记忆”需要在 5 秒内点击两次才会执行。
- “载入隔离演示数据”会临时显示独立 Mock 存储，不会混入真实视觉记忆；重新启动后恢复显示真实记忆。

## 真机验收建议

- 在书桌观察钥匙，再到玄关观察同一把钥匙；查询必须返回玄关。
- 查询从未观察过的雨伞；必须回答没有可靠记录，不能猜测。
- 重启智能体；历史记忆应恢复。
- 停止记忆后等待超过 30 秒；不得再次调用相机。
- 分别断开网络或拒绝相机/麦克风能力；应用不能崩溃，历史点击查询仍应可用。

## 常见问题

- `Camera N/A`：当前宿主未暴露相机能力，请在真机和已授权环境中测试。
- `LLM N/A`：当前 Runtime 没有可用的 LanguageModel 配置，视觉观察会停止，但已有记忆仍可查询。
- `ASR N/A`：改用输入框或点击最近物品。
- `TTS N/A`：结果仍会正常显示，只是不播报。
- 识别结果不是 JSON：本次 Observation 会被丢弃，不会写入错误记忆；请重试。
