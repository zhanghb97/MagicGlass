# 魔镜 MagicGlass v0.7.3 用户指南

## 它能做什么

魔镜会在你明确开启“记忆”后，周期观察当前视野，把可寻找物品的场景、语义地点和相对位置保存在眼镜本地。之后可输入或说出“我的钥匙在哪里？”，魔镜只返回最近一次可靠记录，不会猜测位置。

## 导入与打包

1. 将本 GitHub 仓库导入 Rokid Craft Web 工作台，工程根目录应直接包含 `AGENTS.md`、`app.js`、`app.json` 和 `pages/`。
2. 先运行 Preview。Web/Craft 环境缺少 Camera、LanguageModel 或 SpeechRecognition 时，首页仍应正常显示；DEBUG 区会将对应能力标为 `N/A`。
3. 在 Craft 中执行 AIX 打包，不需要选择子目录。
4. 上传生成的 AIX 到 MagicGlass 智能体。
5. 在 Rokid AI App 中打开开发者模式，将智能体同步到 Rokid Glasses 后启动“魔镜”。

## 第一次使用

1. 启动后观察 DEBUG 区，真机理想状态为 `Camera Ready · LLM Ready · ASR Ready · TTS Ready`。
2. 点击“立即观察”，看向摆有钥匙、手机或水杯的桌面。
3. 等待提示从“正在观察”变为“记住了 N 个物品”。最近物品区应出现识别结果。
4. 移动到另一个位置，再次观察同一物品，用于验证 Last Seen。
5. 输入“钥匙在哪里”，或点击“语音”后说出该句。
6. 结果卡会展示最后看到时间、场景、具体位置和简短寻找提示。

## 顶部状态

- 最右侧是固定的记忆状态位：“等待记忆”不显示灯；“正在记忆”在文字右侧显示缓慢呼吸灯。
- 记忆状态左侧是固定的识别状态位：空闲时只显示“等待识别”；拍照、视觉识别或语音聆听时，文字右侧显示圆形呼吸灯。
- `CURRENT STATUS` 固定在状态面板左上角，不会随详情行数移动。
- 识别与记忆在顶层状态栏横向排列，文字槽等宽对齐，并共用相同的 8×8px 圆形呼吸灯样式。
- 每组文字与呼吸灯间距为 4px，识别与记忆两组状态之间保留 24px 间距。
- 点击“停止记忆”只会停止后续的自适应拍照；已经开始的识别会继续完成并保存结果。

## 主显示栏优先级

- 默认大字显示“魔镜”，小字显示“在无感知负担下，让用户永远找到东西。”
- 只有语音查找成功，或显式进入最近物品栏时，才显示物品名称、场景、相对位置、最近看到时间和外观描述。
- 退出最近物品栏、开始新查找、切换记忆状态或熄屏时，主显示栏恢复默认内容。

## 眼镜交互

- 首次按确认键进入页面导航模式。
- 使用眼镜的上/下方向操作在按钮和输入框之间移动。
- 当前选中控件会出现白色高亮轮廓；再次按确认键执行操作。
- “最近看到”中的物品也是可选按钮，选中并确认后会直接查询该物品。
- 返回键交给系统默认行为，用于退出当前导航层级或关闭页面。

## 周期记忆

- 点击“开始记忆”后会立即观察一次。同一场景连续出现时，后续间隔会从 30 秒逐步增加到 60、120、180 秒。
- 眼镜检测到视角移动后，会等待姿态重新稳定约 2 秒再观察，以及时记录新场景并减少运动模糊。
- 点击“停止记忆”会立即停止周期观察。
- 页面隐藏、退出或卸载时会停止 Timer 和语音能力。当前版本不承诺后台持续拍摄。
- 如果上一轮仍在拍摄或分析，下一次周期任务会跳过，绝不并发采集。
- 每张照片最多记录 5 个关键物品，优先保留钥匙、手机、钱包、眼镜、包、文件和药品等容易忘记位置的物品。
- 家具、固定设施、装饰、墙地面和普通背景杂物不会进入记忆。
- 只保留有明确个人使用情境依据的物品；他人正在穿戴或手持的物品、公共/共享物品和归属不明物品会被丢弃。单张画面无法绝对证明所有权，因此策略会宁可少记，不会猜测。

## 无感熄屏

- 仅在“正在记忆”时启用；连续 10 秒没有按键或点击操作，界面会用 1.2 秒渐变透明。
- 熄屏只隐藏界面，不会停止自适应记忆或中断正在进行的识别。
- 单击确认键、Rokid 眼镜单击键，或点击页面即可恢复显示。
- 唤醒用的第一次单击只恢复界面，不会同时触发当前焦点按钮。
- 渐隐完成后，所有功能按钮会从页面树移除；唤醒按键释放并被拦截后才恢复界面，因此唤醒当下没有可被误触的焦点功能。
- 正式进入熄屏时会取消当前按钮/物品选中状态，并退出最近物品子级导航。唤醒后页面保持无焦点，需再按一次确认键进入导航，然后重新选择。
- 最近物品缩略栏不再依赖原生 `scroll-view`，改为始终可重建的普通视图裁剪窗口，熄屏唤醒后数据仍会从本地存储刷新。
- 进入熄屏时列表位移与子项焦点会归零，但主卡片和最近物品列表始终保留在页面树中。熄屏时卡片改为非导航 `group`，并临时移除两个按钮和最近物品入口；唤醒后只恢复这些交互控件，不再重建列表。
- 缩略栏高度固定为 80px，且任何时候只渲染两行 40px 物品。方向键滚动时替换这两行的数据，不再通过 `transform` 移动整条列表，避免 AIUI 裁剪失效时向外溢出。

## 隐私与数据

- 拍摄图片仅用于当次视觉分析，不长期保存。
- 本地仅保存结构化 Observation，最多 200 条，超出后自动移除最旧记录。
- DEBUG 区“清空真实记忆”需要在 5 秒内点击两次才会执行。
- “载入隔离演示数据”会临时显示独立 Mock 存储，不会混入真实视觉记忆；重新启动后恢复显示真实记忆。

## 真机验收建议

- 在书桌观察钥匙，再到玄关观察同一把钥匙；查询必须返回玄关。
- 查询从未观察过的雨伞；必须回答没有可靠记录，不能猜测。
- 重启智能体；历史记忆应恢复。
- 停止记忆后等待超过当前调度间隔；不得再次调用相机。
- 分别断开网络或拒绝相机/麦克风能力；应用不能崩溃，历史点击查询仍应可用。

## 常见问题

- `Camera N/A`：当前宿主未暴露相机能力，请在真机和已授权环境中测试。
- `LLM N/A`：当前 Runtime 没有可用的 LanguageModel 配置，视觉观察会停止，但已有记忆仍可查询。
- `ASR N/A`：改用输入框或点击最近物品。
- `TTS N/A`：结果仍会正常显示，只是不播报。
- 识别结果不是 JSON：本次 Observation 会被丢弃，不会写入错误记忆；请重试。
